Simulated multitasking on a single CPU/core. It keeps switching execution between apps so effectively at any time only one app is running.

When a program fails to yield execution for any reason, no other program can continue executing. To combat this issue developers came up with [[Preemptive multitasking]].