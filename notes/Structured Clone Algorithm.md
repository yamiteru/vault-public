Mechanism for copying objects. One side has to serialize and the other side has to deserialize the data.

Such cloning is used in JavaScript for communication between [[Web Workers]] on `#Worker.postMessage` call.